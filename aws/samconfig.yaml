# SAM Configuration File
version: 0.1

# Parameters for deployment - can be overridden at deploy time
parameters:
  SpfxOrigin: 'https://*.sharepoint.com'
  Environment: 'prod'
  LambdaMemorySize: 512
  LambdaTimeout: 30

# Build configuration
build:
  # Use esbuild for faster compilation
  typescript_layer: false

# Stack output configuration
output:
  # Store important values for later reference
  stack_name: 'spfx-knowledge-agent'
  template_key_id: null

# Metadata about the application
metadata:
  # Project information
  project_name: 'SPFx Knowledge Agent'
  region: 'us-east-1'
  account_id: '713181485547'
  environment: 'prod'
